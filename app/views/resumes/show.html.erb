<div class="resume-header">
  <h1>
    Rubén Gil
  </h1>
</div>
<div class="tools">
  <%= link_to "Refresh", resumes_refresh_path %>
</div>
<div class="resume">
  <section class="section">
    <section class="contact">
      <p class="name"><%= @user_profile.first_name %> <%= @user_profile.last_name %></p>
      <p class="email"><%= @user_profile.email %></p>
      <p class="phone">34620901135</p>
      <p class="phone">34936331111</p>
    </section>
  </section>
  <section class="picture">
  </section>
  <section class="section">
    <h1>Summary</h1>
    <div class="content">
      <p><%= @user_profile.summary %></p>
    </div>
  </section>
  <%= render partial: 'section', locals: { header: "Experience", collection: @user_profile.positions } %>
  <%= render partial: 'section', locals: { header: "Projects", collection: @user_profile.projects } %>
  <%= render partial: 'section', locals: { header: "Education", collection: @user_profile.educations } %>
</div>
